description: >
  Landmark-weight sweep point: confirmed CNS default with landmark_weight
  raised 0.5 -> 0.8 (stronger global frame). A/B against the default (0.5) and
  w0.3. Everything else identical.
extends: cns_sparselocal_canon_priorreg_noposfb
wandb_run_name: scgg_cns_landmark_w0.8_e1000_seed0
override:
  model.loss.sparse_local_distance.landmark_weight: 0.8
